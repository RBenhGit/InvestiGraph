TWELVE_DATA_API_KEY=your_key_here
# PORT=3210 (Default is 3210 to avoid collisions with 3000/3001/3100 used by other projects)
# CACHE_DIR_PATH=./cache (Default is ./cache under the project root)
# HISTORY_FILE_PATH=./history.json (Default is ./history.json under the project root)
