# משימות — תיקון ממצאי הערכת הפרויקט

**נוצר:** 2026-08-16
**מקור:** `docs/Project_ReEvaluation_2026-08-16.md`
**בסיס מאומת:** 77/77 טסטים עוברים, `eslint` נקי, `tsc` נקי (הורץ דרך ה‑SSH המתועד, לא דרך `Z:`)

## איך להשתמש בקובץ הזה

כל משימה עצמאית וניתנת לביצוע בהקשר נקי. הסדר הוא לפי עדיפות.
משימות 1–5 הן **תיעוד בלבד** — אפס שינוי התנהגות, אפס סיכון לטסטים.
משימה 6 היא **שינוי התנהגות** ודורשת החלטה שלך לפני ביצוע.

סמן `[x]` בסיום. אחרי כל משימה: להריץ `npm test` (דרך ה‑SSH) ולוודא 77/77.

---

## קבוצה א׳ — תיעוד בלבד (ללא סיכון)

### [x] משימה 1 — לתעד את `src/data/yahoo/` ב‑CLAUDE.md
**עדיפות:** גבוהה ביותר — זהו הפער המשמעותי ביחיד בדוח.
**קובץ:** `CLAUDE.md` (סעיף Architecture + דיאגרמת התיקיות)

מה חסר: מודול נתונים שלם ומיושם לגמרי לא מוזכר כלל. מי שקורא את `CLAUDE.md` היום מסיק
ש‑Twelve Data הוא מקור הנתונים היחיד.

מה להוסיף:
- שורה בדיאגרמה: `src/data/yahoo/  fetchAnalystConsensus(ticker) -> AnalystConsensusResult`
- פסקה בסעיף Architecture המתארת: נקודת כניסה יחידה `index.ts` (כמו ב‑`twelvedata/`),
  אף אחד מחוץ למודול לא מייבא `client.ts`/`types.ts`
- שהמודול **לא דורש מפתח API** (endpoint ציבורי) — ולכן היעדרו מ‑`.env.example` מכוון
- להוסיף `yahoo-finance2` לתלויות המוזכרות
- **החוק החשוב:** כישלון של Yahoo חייב להתדרדר ל‑`null` ולעולם לא להכשיל הערכת שווי.
  כרגע הכלל הזה מוגן רק ע"י הערה בקוד וטסט שרת אחד — היא צריכה להיות בתיעוד,
  באותה רמה שבה `growth_estimates` 403 מתועד בסעיף Gotchas.

**אימות:** קריאה חוזרת של `CLAUDE.md` — האם קורא חדש יבין שיש שני מקורות נתונים?

---

### [x] משימה 2 — לתקן את תיאור `historicalPe` ב‑CLAUDE.md
**עדיפות:** גבוהה — התיעוד מתאר אלגוריתם שלא קיים.
**קבצים:** `CLAUDE.md`, ואולי `src/data/twelvedata/types.ts`

שתי טעויות באותו משפט. `CLAUDE.md` אומר כרגע:
> "computes trailing 1y/3y/5y **average** P/E locally from **quarterly** EPS + monthly closes"

בפועל, מאז commit `4d54b50`:
- זהו **חציון (median)**, לא ממוצע — ראה `medianWindow` ב‑`historicalPe.ts:48`
- הקלט הוא EPS **שנתי**, לא רבעוני — נקודת P/E אחת לכל שנת כספים

מה לעשות:
1. לתקן את המשפט ב‑`CLAUDE.md` — חציון, EPS שנתי, וכלל `ceil(N/2)` הנקודות התקינות
2. **החלטה נדרשת:** שמות השדות `avg1y/avg3y/avg5y` עדיין אומרים "ממוצע". שתי אפשרויות:
   - (א) להשאיר את השמות ולהוסיף הערה בתיעוד שהשם היסטורי — **מומלץ**, שינוי שם נוגע
     ב‑`types.ts`, `index.ts`, `historicalPe.ts`, הטסטים, ו‑`app.js` בחזית
   - (ב) לשנות ל‑`median1y/3y/5y` — נקי יותר אבל נוגע ב‑6+ קבצים כולל ה‑UI
   אם בוחרים (ב), זו משימה נפרדת עם טסטים משלה, לא חלק מתיקון תיעוד.

**אימות:** להשוות את המשפט המתוקן מול `historicalPe.ts:45-57` שורה מול שורה.

---

### [x] משימה 3 — להוסיף `PORT` ל‑`.env.example`
**עדיפות:** בינונית
**קובץ:** `.env.example`

`PORT` נקרא ב‑`src/web/server.ts:13` אבל לא מופיע בתבנית. זהו המשתנה היחיד שאינו סוד,
ולכן היחיד שבאמת שימושי למשתמש לגלות.

להוסיף (כשורה מוערת, כי יש ברירת מחדל תקינה):
```
# TWELVE_DATA_API_KEY=your_key_here  (כבר קיים)
# PORT=3210
```
עם הערה קצרה: 3210 נבחר כי 3000/3001/3100 תפוסים ע"י פרויקטים אחרים במכונה.

**אימות:** `grep -rn "process.env" src/ | grep -v test` — לוודא ששני המשתנים היחידים
(`TWELVE_DATA_API_KEY`, `PORT`) מכוסים כעת.

---

### [x] משימה 4 — לנסח מחדש את ההערה ב‑`client.ts:98`
**עדיפות:** בינונית — זו מלכודת לעתיד, לא באג.
**קובץ:** `src/data/twelvedata/client.ts`, שורה 98

ההערה נפתחת ב‑`Quarterly income statement, outputsize=4:` וממשיכה בכך שהתוכנית דוחה
`outputsize` **מעל 6**. שני המספרים באותה הערה, וה‑URL שולח `4`.

**הערך `4` נכון ומכוון** — זה כל מה ש‑`resolveTtmEps` צריך (4 רבעונים).
הסיכון: `CURRENT_WORK.md` מתעד שבעבר **הונח בטעות** שהתקרה היא 4. לכן קורא עתידי עלול
להסיק שה‑`4` הוא הערך המיושן ו"לתקן" אותו ל‑6 — שינוי מיותר שמגדיל את גודל התשובה ללא תועלת.

מה לעשות: לנסח כך שיהיה מפורש ש‑4 הוא **בחירה מכוונת** (הצורך המינימלי), ו‑6 הוא **התקרה**
של התוכנית — שני דברים נפרדים, לא סתירה.

**אימות:** ה‑URL בשורה 110 חייב להישאר `outputsize=4`. לא לשנות קוד, רק הערה.

---

### [x] משימה 5 — להבהיר את הענף המת ב‑`resolveTtmEps` ואת שרשרת ה‑fallback הכפולה
**עדיפות:** נמוכה — תיעוד מדויק יותר, לא תיקון.
**קבצים:** `CLAUDE.md`, `src/data/twelvedata/normalize.ts` (הערה בלבד)

שני פריטים קטנים:

**א.** התיעוד אומר ש‑`resolveTtmEps` "מעדיף את ה‑EPS הנקודתי של ה‑API". אבל `index.ts:107`
תמיד מעביר `null` בפרמטר `apiEps` (מסיבה טובה, מוסברת בהערה שם) — כך שהענף המועדף
**לעולם לא רץ בייצור**. הפונקציה נכונה ומכוסה בטסטים; רק התיאור מרמז על העדפה שאף קורא
לא מפעיל. להוסיף משפט הבהרה.

**ב.** שרשרת ה‑fallback של שיעור הצמיחה (`analystEstimate5y ?? historical3y ?? historical1y`)
**משוכפלת** ב‑`cli/index.ts:41` וב‑`web/public/app.js:290`. לא שווה להפשיט עבור שני מקומות,
אבל שווה הערה בשני הצדדים כדי ששינוי באחד יזכיר את השני.

---

## קבוצה ב׳ — דורש החלטה (שינוי התנהגות)

### [ ] משימה 6 — הפער בין ה‑CLI לאתר: נתוני אנליסטים
**עדיפות:** דורש החלטה שלך לפני ביצוע.
**סטטוס:** ⚠️ לא לבצע לפני שתחליט בין שתי האפשרויות למטה.

הממצא: `CLAUDE.md` טוען שה‑CLI והאתר נבדלים "רק באופן שבו הם אוספים קלט ומעבדים פלט".
זה כבר לא נכון — ל‑CLI יש **אפס** התייחסויות ל‑yahoo, כלומר פאנל האנליסטים הוא web-only.

שתי דרכים לסגור את הפער, והן שונות מהותית:

**אפשרות א׳ — לתקן את התיעוד (זול, ללא סיכון)**
פשוט לכתוב ב‑`CLAUDE.md` שנתוני האנליסטים זמינים באתר בלבד, ולהסביר למה.
זו הרחבה של משימה 1 ולא דורשת שינוי קוד כלל.

**אפשרות ב׳ — להוסיף את הנתונים ל‑CLI (תוספת פיצ'ר)**
לחווט `fetchAnalystConsensus` גם ב‑`src/cli/index.ts` ולהציג ב‑`formatOutput.ts`.
דורש: קריאה מקבילה עם אותה התדרדרות ל‑`null`, פורמט טקסטואלי חדש, וטסטים חדשים
ב‑`formatOutput.test.ts`. זו **עבודת פיצ'ר**, לא תיקון של ממצא — הדוח זיהה חוסר עקביות
בתיעוד, לא באג בקוד.

**המלצתי:** אפשרות א׳ — אלא אם אתה באמת משתמש ב‑CLI ורוצה שם את הנתונים. הדוח לא מצא
שום דבר שבור; ה‑CLI עובד נכון, הוא פשוט מציע פחות.

---

## קבוצה ג׳ — אופציונלי

### [ ] משימה 7 — ליצור `README.md`
**עדיפות:** נמוכה
לא קיים `README.md` בפרויקט. `CLAUDE.md` משרת היטב את Claude, אבל אדם שמשכפל את המאגר
אין לו נקודת כניסה להתקנה והרצה. תוכן מינימלי: מה הכלי עושה, `npm install`, הגדרת
`.env`, `npm run cli -- AAPL`, `npm run web`.

### [ ] משימה 8 — פערי כיסוי טסטים (לשיקול, לא חובה)
הדוח זיהה שלושה מקומות ללא טסטים ישירים:
- `src/data/yahoo/client.ts` — עטיפה דקה, אבל **כאן חיה ההנחה על `+1y` מול `+5y`**,
  והיא נושאת משמעות עבור הפאנל. הכי שווה מבין השלושה.
- `src/cli/index.ts` — חמשת הענפים של `formatStockDataError` לא נבדקים
- `src/web/public/app.js` — אין תשתית טסטים לחזית כלל

אלה עקביים עם ההגדרה של המתאמים כ"דקים" ולא בהכרח דורשים פעולה.

---

## סיכום מצב

| קבוצה | משימות | סיכון | דורש החלטה |
|-------|--------|-------|-------------|
| א׳ — תיעוד | 1–5 | ללא | לא (למעט שמות שדות במשימה 2) |
| ב׳ — התנהגות | 6 | בינוני | **כן** |
| ג׳ — אופציונלי | 7–8 | ללא | לא |

**חשוב:** אף אחד מהממצאים אינו באג בקוד רץ. הבסיס ירוק (77/77, lint, build), והאפליקציה
עובדת נכון. כל הממצאים הם סחיפת תיעוד, פער קונפיגורציה אחד, והערה מבלבלת אחת.
